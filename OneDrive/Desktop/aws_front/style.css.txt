:root {
  --bg: #050505;
  --bg-elevated: #0a0a0a;
  --bg-card: #0f0f0f;
  --text: #ffffff;
  --text-soft: #888;
  --purple: #9966FF;
  --purple-light: #b388ff;
  --purple-soft: rgba(153, 102, 255, 0.12);
  --purple-glow: rgba(153, 102, 255, 0.35);
  --border: #1a1a1a;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

html {
  scroll-behavior: smooth;
}

body {
  font-family: 'Inter', -apple-system, sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.6;
  -webkit-font-smoothing: antialiased;
}

/* Nav */
.nav {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  z-index: 100;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 1rem 2rem;
  background: rgba(10, 10, 10, 0.8);
  backdrop-filter: blur(12px);
  border-bottom: 1px solid var(--border);
}

.logo {
  display: flex;
  align-items: center;
  gap: 0.6rem;
  text-decoration: none;
}

/* E-Square Logo - 4 Es forming a square */
.e-square-logo {
  width: 28px;
  height: 28px;
}

.e-square-logo svg {
  width: 100%;
  height: 100%;
}

.e-square-logo.sm {
  width: 18px;
  height: 18px;
}

.aws-logo {
  height: 22px;
  color: var(--text);
}

.logo-text {
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 600;
  font-size: 0.9rem;
  color: var(--text);
  letter-spacing: -0.01em;
}

.nav-links {
  display: flex;
  gap: 2rem;
}

.nav-links a {
  color: var(--text-soft);
  text-decoration: none;
  font-size: 0.85rem;
  transition: color 0.2s;
}

.nav-links a:hover {
  color: var(--text);
}

.nav-btn {
  background: var(--purple);
  color: white;
  padding: 0.5rem 1rem;
  border-radius: 6px;
  text-decoration: none;
  font-size: 0.8rem;
  font-weight: 500;
  transition: all 0.2s;
}

.nav-btn:hover {
  background: #8855ee;
  transform: translateY(-1px);
}

.burger {
  display: none;
  background: none;
  border: none;
  width: 24px;
  height: 18px;
  cursor: pointer;
  position: relative;
}

.burger span,
.burger::before,
.burger::after {
  content: '';
  position: absolute;
  left: 0;
  width: 100%;
  height: 2px;
  background: var(--text);
  transition: 0.2s;
}

.burger::before { top: 0; }
.burger span { top: 50%; transform: translateY(-50%); }
.burger::after { bottom: 0; }

/* Buttons */
.btn {
  display: inline-block;
  padding: 0.65rem 1.25rem;
  font-size: 0.85rem;
  font-weight: 500;
  text-decoration: none;
  border-radius: 6px;
  transition: all 0.2s;
  cursor: pointer;
  border: none;
  font-family: inherit;
}

.btn.primary {
  background: var(--purple);
  color: white;
}

.btn.primary:hover {
  background: #8855ee;
  transform: translateY(-2px);
  box-shadow: 0 8px 20px var(--purple-glow);
}

.btn.secondary {
  background: transparent;
  color: var(--text);
  border: 1px solid var(--border);
}

.btn.secondary:hover {
  border-color: var(--text-soft);
}

.btn.lg {
  padding: 0.85rem 1.75rem;
  font-size: 0.95rem;
}

/* Hero */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 8rem 4rem 4rem;
  gap: 4rem;
  overflow: hidden;
}

.hero-glow {
  position: absolute;
  top: 20%;
  right: 15%;
  width: 400px;
  height: 400px;
  background: radial-gradient(circle, var(--purple-glow) 0%, transparent 70%);
  filter: blur(80px);
  pointer-events: none;
}

.hero-content {
  max-width: 480px;
  position: relative;
  z-index: 1;
}

.tag {
  display: inline-block;
  background: var(--purple-soft);
  color: var(--purple);
  font-size: 0.75rem;
  font-weight: 500;
  padding: 0.35rem 0.75rem;
  border-radius: 4px;
  margin-bottom: 1.25rem;
}

.hero h1 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: clamp(2.25rem, 4.5vw, 3rem);
  font-weight: 700;
  line-height: 1.15;
  margin-bottom: 1.25rem;
  letter-spacing: -0.02em;
}

.hero-sub {
  color: var(--text-soft);
  font-size: 1rem;
  max-width: 380px;
  margin-bottom: 1.75rem;
  line-height: 1.7;
}

.hero-btns {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
}

/* Cube grid visual */
.hero-visual {
  position: relative;
  z-index: 1;
}

.cube-grid {
  display: grid;
  grid-template-columns: repeat(3, 50px);
  gap: 8px;
  transform: perspective(600px) rotateX(10deg) rotateY(-10deg);
}

.cube {
  width: 50px;
  height: 50px;
  background: var(--purple-soft);
  border: 1px solid rgba(153, 102, 255, 0.3);
  border-radius: 6px;
  transition: all 0.3s;
}

.cube:hover {
  background: var(--purple);
  transform: translateY(-4px);
  box-shadow: 0 8px 16px var(--purple-glow);
}

.cube.c1 { animation: pulse 3s ease-in-out infinite; }
.cube.c3 { animation: pulse 3s ease-in-out 0.5s infinite; }
.cube.c5 { animation: pulse 3s ease-in-out 1s infinite; background: var(--purple); }
.cube.c7 { animation: pulse 3s ease-in-out 1.5s infinite; }
.cube.c9 { animation: pulse 3s ease-in-out 2s infinite; }

@keyframes pulse {
  0%, 100% { opacity: 0.6; }
  50% { opacity: 1; }
}

/* Stats */
.stats {
  display: flex;
  justify-content: center;
  gap: 4rem;
  padding: 2.5rem 2rem;
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.stat {
  text-align: center;
}

.stat-num {
  display: block;
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.75rem;
  font-weight: 700;
  color: var(--text);
  margin-bottom: 0.15rem;
}

.stat-label {
  font-size: 0.8rem;
  color: var(--text-soft);
}

/* About */
.about {
  padding: 5rem 2rem;
  max-width: 900px;
  margin: 0 auto;
}

.about-header {
  text-align: center;
  max-width: 500px;
  margin: 0 auto 2.5rem;
}

.about-header h2,
.domains h2,
.events h2,
.gallery h2,
.join h2 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  margin-bottom: 0.5rem;
}

.about-header p {
  color: var(--text-soft);
  font-size: 0.95rem;
}

.cards {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 1rem;
}

.card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 1.5rem;
  transition: all 0.2s;
}

.card:hover {
  border-color: var(--purple);
  transform: translateY(-2px);
}

.card-icon {
  width: 36px;
  height: 36px;
  background: var(--purple-soft);
  border-radius: 6px;
  display: flex;
  align-items: center;
  justify-content: center;
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 700;
  font-size: 1rem;
  color: var(--purple);
  margin-bottom: 0.85rem;
}

.card h3 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  margin-bottom: 0.35rem;
}

.card p {
  color: var(--text-soft);
  font-size: 0.85rem;
  line-height: 1.5;
}

/* Domains */
.domains {
  padding: 3rem 2rem;
  text-align: center;
  background: var(--bg-elevated);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.domain-pills {
  display: flex;
  justify-content: center;
  flex-wrap: wrap;
  gap: 0.5rem;
  max-width: 500px;
  margin: 1rem auto 0;
}

.pill {
  background: var(--bg-card);
  border: 1px solid var(--border);
  padding: 0.45rem 0.9rem;
  border-radius: 4px;
  font-size: 0.8rem;
  color: var(--text-soft);
  transition: all 0.2s;
  cursor: default;
}

.pill:hover {
  border-color: var(--purple);
  color: var(--purple);
}

/* Events */
.events {
  padding: 5rem 2rem;
  max-width: 650px;
  margin: 0 auto;
}

.events h2 {
  text-align: center;
  margin-bottom: 2rem;
}

.events-list {
  display: flex;
  flex-direction: column;
  gap: 0.75rem;
}

.event {
  display: flex;
  align-items: center;
  gap: 1rem;
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 8px;
  padding: 1rem 1.25rem;
  transition: border-color 0.2s;
}

.event:hover {
  border-color: var(--text-soft);
}

.event-date {
  display: flex;
  flex-direction: column;
  align-items: center;
  min-width: 42px;
}

.event-date .day {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.25rem;
  font-weight: 700;
  line-height: 1;
}

.event-date .month {
  font-size: 0.65rem;
  color: var(--text-soft);
  text-transform: uppercase;
  letter-spacing: 0.5px;
}

.event-info {
  flex: 1;
}

.event-info h3 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 0.9rem;
  font-weight: 600;
  margin-bottom: 0.15rem;
}

.event-info p {
  color: var(--text-soft);
  font-size: 0.8rem;
}

.event-status {
  font-size: 0.65rem;
  font-weight: 500;
  text-transform: uppercase;
  letter-spacing: 0.5px;
  padding: 0.3rem 0.6rem;
  border-radius: 4px;
  background: var(--bg);
  color: var(--text-soft);
}

.event-status.open {
  background: rgba(34, 197, 94, 0.15);
  color: #22c55e;
}

.event-status.soon {
  background: var(--purple-soft);
  color: var(--purple);
}

/* Gallery */
.gallery {
  padding: 4rem 2rem;
  max-width: 800px;
  margin: 0 auto;
}

/* Why Join */
.why-join {
  padding: 5rem 2rem;
  max-width: 900px;
  margin: 0 auto;
}

.why-join h2 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  text-align: center;
  margin-bottom: 2.5rem;
}

.why-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: 1.25rem;
}

.why-card {
  background: var(--bg-card);
  border: 1px solid var(--border);
  border-radius: 10px;
  padding: 1.5rem;
  transition: all 0.2s;
}

.why-card:hover {
  border-color: var(--purple);
}

.why-num {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 0.75rem;
  font-weight: 700;
  color: var(--purple);
  margin-bottom: 0.75rem;
}

.why-card h3 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1rem;
  font-weight: 600;
  margin-bottom: 0.35rem;
}

.why-card p {
  color: var(--text-soft);
  font-size: 0.85rem;
  line-height: 1.5;
}

/* Timeline */
.timeline {
  padding: 5rem 2rem;
  max-width: 700px;
  margin: 0 auto;
  background: var(--bg-elevated);
  border-top: 1px solid var(--border);
  border-bottom: 1px solid var(--border);
}

.timeline h2 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 1.5rem;
  font-weight: 700;
  text-align: center;
  margin-bottom: 2.5rem;
}

.timeline-track {
  position: relative;
  padding-left: 2rem;
}

.timeline-track::before {
  content: '';
  position: absolute;
  left: 5px;
  top: 0;
  bottom: 0;
  width: 2px;
  background: var(--border);
}

.timeline-item {
  position: relative;
  padding-bottom: 2rem;
}

.timeline-item:last-child {
  padding-bottom: 0;
}

.timeline-dot {
  position: absolute;
  left: -2rem;
  top: 0.25rem;
  width: 12px;
  height: 12px;
  background: var(--bg);
  border: 2px solid var(--border);
  border-radius: 50%;
}

.timeline-dot.active {
  background: var(--purple);
  border-color: var(--purple);
  box-shadow: 0 0 12px var(--purple-glow);
}

.timeline-content {
  padding-left: 0.5rem;
}

.timeline-date {
  font-size: 0.7rem;
  font-weight: 500;
  color: var(--purple);
  text-transform: uppercase;
  letter-spacing: 0.5px;
  display: block;
  margin-bottom: 0.25rem;
}

.timeline-content h3 {
  font-family: 'Space Grotesk', sans-serif;
  font-size: 0.95rem;
  font-weight: 600;
  margin-bottom: 0.2rem;
}

.timeline-content p {
  color: var(--text-soft);
  font-size: 0.8rem;
}

.gallery h2 {
  text-align: center;
  margin-bottom: 1.5rem;
}

.gallery-grid {
  display: grid;
  grid-template-columns: 2fr 1fr;
  grid-template-rows: repeat(2, 120px);
  gap: 0.75rem;
}

.gallery-item {
  background: linear-gradient(135deg, var(--bg-card) 0%, var(--bg-elevated) 100%);
  border: 1px solid var(--border);
  border-radius: 8px;
  display: flex;
  align-items: flex-end;
  padding: 0.85rem;
  transition: all 0.2s;
}

.gallery-item:hover {
  border-color: var(--purple);
  transform: scale(1.01);
}

.gallery-item span {
  font-size: 0.75rem;
  color: var(--text-soft);
}

.gallery-item.g1 {
  grid-row: span 2;
}

.gallery-item.g4 {
  grid-column: span 2;
}

/* Join */
.join {
  padding: 4rem 2rem;
  background: linear-gradient(180deg, var(--bg) 0%, var(--bg-elevated) 100%);
  text-align: center;
  border-top: 1px solid var(--border);
}

.join-content {
  max-width: 360px;
  margin: 0 auto;
}

.join p {
  color: var(--text-soft);
  margin-bottom: 1.25rem;
}

/* Footer */
.footer {
  padding: 2rem;
  border-top: 1px solid var(--border);
}

.footer-inner {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 1rem;
}

.footer-brand {
  display: flex;
  align-items: center;
  gap: 0.4rem;
}

.footer-brand span {
  font-family: 'Space Grotesk', sans-serif;
  font-weight: 600;
  font-size: 0.8rem;
  color: var(--text-soft);
}

.aws-logo-sm {
  height: 16px;
  color: var(--text-soft);
}

.footer-links {
  display: flex;
  gap: 1.25rem;
}

.footer-links a {
  color: var(--text-soft);
  text-decoration: none;
  font-size: 0.8rem;
  transition: color 0.2s;
}

.footer-links a:hover {
  color: var(--text);
}

.footer-copy {
  font-size: 0.75rem;
  color: var(--text-soft);
}

/* Mobile */
@media (max-width: 900px) {
  .cards {
    grid-template-columns: 1fr;
  }
  
  .why-grid {
    grid-template-columns: 1fr;
  }
  
  .gallery-grid {
    grid-template-columns: 1fr 1fr;
    grid-template-rows: auto;
  }
  
  .gallery-item.g1,
  .gallery-item.g4 {
    grid-row: auto;
    grid-column: auto;
  }
}

@media (max-width: 768px) {
  .nav {
    padding: 0.85rem 1.25rem;
  }
  
  .nav-links {
    display: none;
    position: absolute;
    top: 100%;
    left: 0;
    right: 0;
    flex-direction: column;
    background: var(--bg);
    padding: 1.25rem;
    border-bottom: 1px solid var(--border);
    gap: 0.85rem;
  }
  
  .nav-links.open {
    display: flex;
  }
  
  .nav-btn {
    display: none;
  }
  
  .burger {
    display: block;
  }
  
  .hero {
    flex-direction: column;
    padding: 6rem 1.5rem 3rem;
    text-align: center;
  }
  
  .hero-content {
    max-width: 100%;
  }
  
  .hero-sub {
    max-width: 100%;
  }
  
  .hero-btns {
    justify-content: center;
  }
  
  .hero-glow {
    top: 10%;
    right: auto;
    left: 50%;
    transform: translateX(-50%);
    width: 300px;
    height: 300px;
  }
  
  .cube-grid {
    transform: perspective(600px) rotateX(5deg) rotateY(-5deg);
  }
  
  .stats {
    gap: 2rem;
  }
  
  .stat-num {
    font-size: 1.5rem;
  }
  
  .event {
    flex-wrap: wrap;
  }
  
  .event-status {
    margin-left: auto;
  }
}

@media (max-width: 480px) {
  .stats {
    flex-direction: column;
    gap: 1.25rem;
  }
  
  .gallery-grid {
    grid-template-columns: 1fr;
  }
  
  .footer-inner {
    flex-direction: column;
    gap: 1rem;
    text-align: center;
  }
}